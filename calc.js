

const summation = (a, b) =>  a + b;
const subtraction = (a, b) =>  a - b;
const multiply = (a, b) => a * b;
const division = (a, b) =>  a / b;
 
module.exports = {
  summation,
  subtraction,
  multiply,
  division
}
